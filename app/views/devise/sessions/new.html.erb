<div class="container">
  <div class="row my-4 justify-content-start">
    <h4 class="col-lg-4 py-2 mb-0 text-center border border-dark rounded">ログイン</h4>
  </div>
</div>
<div class="container py-4 border border-dark rounded bg-light">
  <%= form_with model:@user, url: user_session_path, local: true do |f| %>
    <div class="row pt-2 justify-content-center">
      <div class="form-group col-6 my-3">
        <%= f.email_field :email, autofocus: true, :placeholder => "メールアドレス", autocomplete: "email",class: "form-control" %>
      </div>
    </div>

    <div class="row pt-2 justify-content-center">
      <div class="form-group col-6 my-3">
        <%= f.password_field :password, :placeholder => "パスワード(6文字以上)", autocomplete: "current-password",class: "form-control" %>
      </div>
    </div>

    <div class="row pt-2 justify-content-center">
      <div class="form-group col-6 my-3">
        <%= f.password_field :password_confirmation, :placeholder => "パスワード(確認用)", autocomplete: "new-password",class: "form-control" %>
      </div>
    </div>

    <div class="row pt-2 justify-content-center">
      <div class="form-group col-6 my-3">
        <%= f.submit "ログイン", class: "col-4 btn btn-outline-dark" %>
      </div>
    </div>
  <% end %>
  <div class="row justify-content-center">
    <div class="col-6 rounded">
      新規登録がお済でない方は
      <span class="px-4 bg-secondary"><%= render "devise/shared/links" %></span><br />
      から新規登録を行ってください。
    </div>
  </div>
</div>
