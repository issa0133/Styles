<div>
  <p>ジャンル検索</p>
  <% genres.each do |genre| %>
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.hidden_field :model, value: "Genre" %>
      <%= f.hidden_field :word, value: genre.id  %>
      <%= f.submit genre.name %>
    <% end %>
  <% end %>
</div>